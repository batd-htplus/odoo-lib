# Python packages required by the custom / third-party addons in ./addons.
# Derived from the actual imports found in the addon sources - keep in sync.
#
#   openpyxl        htplus_aps_core, htplus_mes_shopfloor  (xlsx import/export)
#   xlsxwriter      htplus_aps_core                        (xlsx reports)
#   xlrd            legacy .xls import
#   paramiko        wk_backup_restore (vendor)             (SFTP off-site backup)
#   python-crontab  wk_backup_restore (vendor)             (system cron entries)
#   pandas          dashboard aggregation
#   requests        htplus_planning_bridge                 (also an Odoo dep)
#
# Versions are pinned to keep builds reproducible. Bump deliberately.

openpyxl==3.1.5
XlsxWriter==3.2.0
xlrd==2.0.1
paramiko==3.5.0
python-crontab==3.2.0
pandas==2.2.3
requests==2.32.3
